sample_project:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: serious-music-469407-f1
      dataset: banking
      threads: 4

      keyfile: "{{ env_var('GOOGLE_APPLICATION_CREDENTIALS') }}"
